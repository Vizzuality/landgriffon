import cx from 'classnames';

const AppScreens: React.FC = () => {
  return (
    <div
      className={cx({
        'grid grid-cols-3 gap-10': true,
      })}
    >
      <div></div>
    </div>
  );
};

export default AppScreens;
