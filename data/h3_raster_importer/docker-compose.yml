version: "3.8"
services:
    landgriffon-seed-data:
        build:
            context: ./
            dockerfile: Dockerfile
        volumes:
            - '../data/seed:/data'
        command: pip freeze
        env_file:
            - '../../.env'
        
networks:
    default:
        external: true
        name: landgriffon_default